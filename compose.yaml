services:
  main:
    build:
      context: main_server
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    networks:
      - mynetwork

  secondary-1:
    build:
      context: lev_secondary_server
      dockerfile: Dockerfile
    ports:
      - "7000:8000"
    networks:
      - mynetwork

  secondary-2:
    build:
      context: lev_secondary_server
      dockerfile: Dockerfile
    ports:
      - "7001:8000"
    networks:
      - mynetwork



networks:
  mynetwork:
    driver: bridge

